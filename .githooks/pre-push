#!/usr/bin/env bash
# Check the pushed revision, dependencies, and public documentation links.
set -euo pipefail
TASK_ROOT="$(git rev-parse --show-toplevel)"
readonly TASK_ROOT
cd "${TASK_ROOT}"
exec mise run hook:pre-push -- "$@"
