#!/usr/bin/env bash
# Runtime: Bash 3.2+, macOS and Linux.
# Validate the supplied message without rewriting it.
set -euo pipefail
TASK_ROOT="$(git rev-parse --show-toplevel)"
readonly TASK_ROOT
cd "${TASK_ROOT}" || exit 1
exec mise run hook:commit-msg -- "$@"
