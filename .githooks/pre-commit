#!/usr/bin/env bash
#
# Check staged secrets and working-tree source without rewriting files.
# Runtime: Bash 3.2+, macOS and Linux.
set -euo pipefail

# main - Check staged secrets and working-tree source without rewriting files.
main() {
  TASK_ROOT="$(git rev-parse --show-toplevel)"
  readonly TASK_ROOT
  cd "${TASK_ROOT}" || exit 1
  exec mise run repo:hook:pre-commit
}

main "$@"
